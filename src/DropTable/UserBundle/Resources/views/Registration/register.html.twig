{% extends "DropTableUserBundle:Default:base.html.twig" %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-3 text-right verticalMiddle" id="register">
                {% include "@DropTableUser/Registration/register_content.html.twig" %}
                <div>
                    Or register with <a href="#" onclick="fb_login();" class="btn btn-primary btn-sm">Facebook</a>
                    or
                    <a href="{{ hwi_oauth_login_url('google') }}" class="btn btn-danger btn-sm">Google</a>
                </div>
            </div>
        </div>
    </div>
{% endblock body %}
